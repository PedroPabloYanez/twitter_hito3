
<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card mx-auto col-md-6 mb-3">
        <h3 class="card-title py-3">Crear nuevo tweet</h3>
        <div class="form-group mx-5 text-start ">
            <%= form.label :content, "Publicación: " %>
            <%= form.text_area :content, class: 'form-control ' %>
        </div>
        <div class=" form-group m-5 text-start">
        <%= form.label :photo, "Imagen: " %>
        <%= form.file_field :photo, class: 'form-control ' %>
        </div> 
        <div class="actions mb-5">
          <%= form.submit "Crear nuevo tweet", :class => 'btn btn-primary' %>
        </div>
    </div>
<% end %>